# Weat API

## Getting Started

### Environment Variables

Create a `.env` file with:

```
DB_USERNAME=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
```

### Running with Docker Compose

Build and start:

```bash
docker-compose up --build -d
```

## License

MIT
